/* esp32-hal-gpio.h -- forwarder. An Arduino-ESP32 core internal header; everything the
 * sources use from it (pinMode/digitalWrite/digitalRead/HIGH/LOW/INPUT/OUTPUT) is already
 * in the shim. Part of the shim, dies with it. */
#pragma once
#include "arduino_compat.h"
