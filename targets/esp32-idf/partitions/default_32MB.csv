# ESP32-S3 32 MB (W25Q256), dual app slots. Used by the reTerminal-class boards
# (s3-n32r8, s3-n32r8-extuart, and s3-e1004 when its panel driver is vendored).
#
# 6.25 MB per slot -- generous, matching default_16MB.csv's shape rather than shrinking the
# app to grow the filesystem. Nothing on this target needs bulk storage since config moved to
# NVS during phase B (hal/od_hal_nvs.c), so the remaining 19 MB of SPIFFS is headroom for
# whatever the reTerminal builds want later rather than a considered allocation.
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xe000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x640000,
app1,       app,  ota_1,    0x650000,  0x640000,
spiffs,     data, spiffs,   0xc90000,  0x1360000,
coredump,   data, coredump, 0x1ff0000, 0x10000,
