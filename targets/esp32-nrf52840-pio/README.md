# Target: ESP32-S3 / C3 / C6 + nRF52840 (PlatformIO)

Source repo: `Firmware` (https://github.com/davelee98/Firmware)

Not yet imported — build from the original repo for now. First target in the migration
order; see ../../docs/MIGRATION.md.

Build (once imported): `pio run -e <env>`
