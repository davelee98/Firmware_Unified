# LM20-only extras (also mirrored in boards/*_cpuapp.conf for sysbuild).
# CONFIG_BT_CTLR_ASSERT_OPTIMIZE_FOR_SIZE is not set

# SoC NFCT Type 2 Tag (N1/N2 antenna pads). Gated at runtime by nfc_config 0x2A.
CONFIG_NFC_T2T_NRFXLIB=y
