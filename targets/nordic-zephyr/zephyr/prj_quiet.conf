# PROFILE=quiet — minimize CPU/debug activity for advertising-current tests.
# Still uses RTT if a probe is attached; disconnect the debugger for µA numbers.
CONFIG_LOG=n
# CONFIG_LOG_BACKEND_RTT is not set
