# Target: XIAO nRF54L15 (Zephyr / nRF Connect SDK)

Source repo: `Firmware_NRF54` (https://github.com/davelee98/Firmware_NRF54)

Not yet imported. Build (once imported): `./build.sh`, flash with `./flash.sh`.
`PROFILE=uart ./build.sh` for USB-serial debug; battery builds log over SEGGER RTT.
